# Build plan — live checklist

**Approved by Abu 2026-09-12.** Tick boxes as work lands. A fresh session reads `DECISIONS.md`,
then this file, then starts at the first unticked slice. Each slice ends with `pnpm build`,
`pnpm typecheck`, `pnpm lint` clean and Abu looking at it running locally (Node 24,
desktop + ~390px). **Abu judges the look — the agent does not rule on taste.**

## Where the evidence lives (read these before building a slice)

| File | What it holds |
|---|---|
| `references/_notes/portaldot-report.md` | Portaldot web end to end: every route, the full token table, every card, wallet modal/pill, and a diff of what the current build changed |
| `references/_notes/masayume-deepbookie-report.md` | Masayume nav/onboarding/modals/toasts/menus and DeepBookie cards/charts/history/launcher, with file:line |
| `references/_notes/engine-platform-report.md` | What `lib/` exports, what v1 has that is not ported, KeeperHub auth/wallet/execute/workflow APIs, "shell must wire to" list |
| `references/_notes/21st/` | Code for 21st components: 20123 tool-calls-section, 12423 tool-group, 3035 chatgpt-prompt-input, 26580 tool-approval, 12402 agent-chat, 12376 generic-tool, 12373 plan-tool, 23789 ai-tool-call. Re-fetch with `npx @21st-dev/cli get <id>` |
| `references/_notes/prior-ledger.md`, `prior-critique.md` | Earlier fidelity extraction + the audit of it. Evidence only — its recommendations were rejected for deciding things on Abu's behalf |
| `docs/FINDINGS.md` | Engine facts (registry counts, voice policy, add-on research) |

Reference roots (pinned): Portaldot `references/portaldot-mcp/packages/web` @ `86699dc` ·
Masayume `references/masayume/web/src` @ `68f7a09` · DeepBookie `references/deepbookie/apps/web/src`
@ `0f1013b` · KeeperHub `references/keeperhub-fork` @ `946eeb5c9` · v1 app
`~/dev/hackathon/keeperhub-copilot` @ `e5d9788`.

**Fidelity rule for every port:** diff the ported file against its reference. Only copy, the
tokens named here, and adapters may differ; list each difference in the slice report. Anything
no reference has is additive and drawn in Portaldot's language. Never fake data — if a real source
is missing, show an honest unavailable/loading state.

---

## 0 · Paperwork
- [x] Rewrite `DECISIONS.md` (Portaldot paint, reference per surface, Connect KeeperHub, consequences).
- [x] Create this file.
- [x] Copy research reports and 21st code into `references/_notes/`.

## 1 · Foundation
- [x] Replace `app/globals.css` with Portaldot's `app/globals.css` verbatim (tokens, `@theme inline`, radius multipliers, easings, durations, `--animate-*`, keyframes, grain, `.perforation`, `.receipt-watermark` (text → KEEPERHUB), `.glow-*`, `.atmos-corners`, reduced motion). Drop the KeeperHub light palette, `--brand*`, `--keeperhub-green*`, `--hub-*` — re-point any class using them.
- [x] `lib/fonts.ts` → Geist + Geist Mono; `app/layout.tsx` → Neue Montreal via Fontshare `<link>`, `html className="dark"` forced.
- [x] Add deps (pnpm 10): `@base-ui/react class-variance-authority tw-animate-css shadcn @react-three/fiber @react-three/drei @react-three/postprocessing react-markdown remark-gfm shiki qrcode.react @dicebear/core motion`.
- [x] `components/ui/`: port Portaldot button, input, separator, sheet, tooltip, skeleton; Masayume menu, dialog, toast, tabs (Base UI).
- [x] Data primitives `Money`, `Hash`, `Address`, `UtcTime` (Masayume structure, DeepBookie `lib/format.ts` rules); `CopyChip`, `ExplorerLink` (Portaldot `tools.tsx`); `Identicon` via @dicebear; `ChainMark`/`TokenMark` (svgl via `npx @21st-dev/cli logo <name>`, DeepBookie `CoinLogo` 3-tier fallback).
- [x] Landing fixes: restore Portaldot `components/landing/hero-wave.tsx` (r3f); remove inlined keyframes/glows now in globals; `brand-deep` → `primary-press`.
- [ ] Abu reviews `/` and `/dev/foundation` running locally (desktop + ~390px).

**Slice 1 report (2026-09-12).** Build, typecheck, lint clean. Review page `/dev/foundation` (noindex, unlinked).
Differences from the references, all deliberate:
- `globals.css`: watermark text KEEPERHUB; header comment. Nothing else.
- Masayume has no menu/dialog *files* (Base UI is used inline). `ui/menu.tsx` = `DesktopNavMenu.tsx` + `navigation.css` grammar in Portaldot's WalletPill-menu paint. `ui/dialog.tsx` = Masayume `modal.css` layout (448px, head / scrolling body / fixed footer, eyebrow, close) inside Portaldot's `wallet-picker.tsx` shell and motion. **Conflict:** Masayume's modal never uses a bottom sheet on phones; Portaldot's picker (and Masayume's own tutorial) does — the sheet is used.
- `ui/toast.tsx`: Masayume behaviour; neutral-900 plate → `bg-card`, profit/loss/warning inks → success/destructive/pending; `--toast-offset` falls back to 1rem until slice 3 sets it.
- React 19 lint adapters, same behaviour: `use-scroll`, `use-mobile` read via `useSyncExternalStore`; hero typewriter reset deferred one tick; install typewriter drops its synchronous reset; hero-wave `useMemo` arrow.
- `Identicon`: DiceBear `identicon` style, default colours (Portaldot's props and halo kept).
- Marks: svgl has only ETH, Polygon, BNB, Solana, Tether → Base, Arbitrum, Optimism, Gnosis, Avalanche, Tempo, USDC render as DeepBookie discs in the chain's own colour.
- Data primitives: Masayume `numbers` → `font-mono tabular-nums`; Hash 8…6, Address 6…4, CopyChip keeps Portaldot's 6…6.
- Deps beyond the list: `postprocessing` (peer of `@react-three/postprocessing`), `@dicebear/identicon` with `@dicebear/core` pinned to Masayume's 9.4.3; `gsap` removed (only the replaced hero used it). Floating nav imports `ui/button`, `ui/use-scroll`, `ui/menu-toggle-icon` like Portaldot instead of inlining them.
- `/api/platform/chains` now reads `KEEPERHUB_URL` through new `getPlatformConfig()` (was a lint error).
- `pnpm test` is not meaningful yet: the app has no tests, and vitest with no config also runs the 1,825 test files inside `references/`. *(Fixed in slice 2.)*

## 2 · Wiring (port from v1)
- [x] `app/api/auth/{_shared,login,callback,logout,reauth}` from v1.
- [x] `app/api/chat/{route,approval-signature,rerun}.ts` and `app/api/chat/simulate/route.ts` from v1.
- [x] `app/api/conversations/**` from v1.
- [x] `app/api/platform/chains` → return the chain list (`GET ${KEEPERHUB_URL}/api/chains`), not just a count.
- [x] `app/api/account` → identity, org wallet (`fetchOrgWalletAddress`), balance on selected network.
- [x] `lib/data/ledger.ts` → add a list query (by org, newest first) for Activity/History.
- [x] `.env.local` + OAuth client — fresh start (Abu, 2026-09-12): own database `keeperhub_v2` (new, empty, on v1's Neon endpoint; v1's `neondb` untouched), own KeeperHub OAuth client "KeeperHub Copilot v2 (local)" `1b1b7945-…` registered for `http://localhost:3001/api/auth/callback` (client_secret_post, mcp:read mcp:write), fresh `SESSION_SECRET` / `TOOL_APPROVAL_SECRET`, `APP_BASE_URL=http://localhost:3001`, OpenAI key reused from v1. Migrations applied (3). Verified: `db:verify` connects; `/api/account` 401s signed out; `/api/auth/login` 307s to KeeperHub with the new client and KeeperHub forwards to its login page (client accepted); `/api/platform/chains` returns 24 networks.
- [x] Abu signs in once locally (http://localhost:3001/api/auth/login → back on `/`). Confirmed 2026-09-12: one session row in `keeperhub_v2`, scope `mcp:read mcp:write`. `/api/account?chainId=84532` not yet eyeballed — it gets exercised by the money pill in slice 3.

**Slice 2 report (2026-09-12).** Typecheck, lint, build clean. `pnpm test`: 399 passing in 30 files.
- The eleven route files are v1's, byte-identical. `lib/` still matches v1 apart from slice 1's additions and the two below.
- Tests: v1's lib and route tests plus `vitest.config.ts` (scoped to `tests/**`, so `references/` no longer runs). Held back: `tests/cards/*` (slice 6) and `tests/conversations/load.test.ts` (imports v1's chat page, slice 5).
- `/api/platform/chains` → `{ count, chains[] }`: enabled only, mainnets first then by name, ids as strings like `lib/chains.ts`. `count` kept for the landing ticker and footer.
- `/api/account?chainId=` → `{ userId, orgId, walletAddress, balance }`, balance `ok | no-network | no-wallet | unavailable`. New `lib/session/balance.ts` reads KeeperHub `GET /api/user/wallet/balances`, which accepts the OAuth token (fork `lib/middleware/auth-helpers.ts:418`), and picks one chain. A chain the platform zero-filled after an RPC error reads as unavailable, not 0. **Known gap:** the platform zero-fills a single failed *token* with no flag, so one token can read 0 wrongly and we cannot tell. `lib/wallet/` untouched (reserved for the agentic payer wallet).
- `listLedgerRows(scope, { limit ≤ 100, before })` in `lib/data/ledger.ts`, on `ledger_entry_org_created_idx`. No unit test (repo convention: no test database).
- The OAuth callback still lands on `/` (v1 behaviour); slice 4 moves it to `/app`.

## 3 · Shell & navigation (Masayume)
- [x] `app/(app)/layout.tsx` with ShellChrome-style layout; nav registry (`nav-items.ts` pattern) with IA: Chat · History · Activity · Build▾ (Automations, Actions) · Learn▾ (Getting started, MCP setup, How it works).
- [x] Header, Base UI mega-menus (Masayume animation, one open at a time, close on route change/≤720px).
- [x] Mobile floating pill (4 + More) and right More drawer; 420px wallet-dot collapse.
- [x] AppStrip (dismissible), Marquee (real data only: chain list + latest receipts, LOADING fallback), grain, Toaster (limit 1).
- [x] Right side: Network pill (+ additive: Base UI Menu with ChainMarks; cookie; read by chat system prompt and card defaults), money pill (org wallet balance via `/api/account?chainId=`), account pill. *(Card defaults read the cookie in slice 6.)*
- [x] Abu reviews `/app` running locally (desktop + ~390px), signed out and signed in. Abu, 2026-09-12: "looking good, let's continue". Strip lines, menu descriptions, phone pill four and the Base Sepolia default kept as built.

**Slice 3 report (2026-09-12).** Typecheck, lint, build clean. `pnpm test`: 421 passing in 34 files (+22).
Checked against the running dev server: `/app`, `/app/history`, `/app/activity`, `/app/automations` and `/` return 200;
`/api/ledger` 401s signed out; `/api/platform/chains` lists 24. The signed-in pills have not been looked at yet.
Files: `app/(app)/{layout.tsx,shell.css}`, `components/shell/**`, `app/api/ledger/route.ts`, `lib/network.ts`.
Differences from the references, all deliberate:
- **Scope.** The shell wraps only `(app)` routes; the landing keeps Portaldot's floating nav. Not mounted: Masayume's Footer, CustomCursor, ThemeToggle (dark only) and GrainOverlay (Portaldot's `body::after` grain already covers every page; both would double it).
- **Paint.** vermilion → `primary`; gray-400 → `fg-secondary`; gray-500 and gray-600 → `fg-muted` (the ticker's label/value greys become one tone); Masayume's hairlines and `--nav-*` tokens → `border` / `border-strong` / `surface-2`; its near-black grounds (#000, 5,5,5/.96, 12,12,12/.72) → `background` at the same opacities.
- **Idiom.** Masayume's CSS classes became Tailwind utilities in the components (the `ui/menu` idiom). What cannot be a utility (`--appstrip`, `--toast-offset`, keyframes) lives in `app/(app)/shell.css`, so `globals.css` stays Portaldot-only. Menus and the drawer close on navigation by remembering the page they opened on, and the strip's dismissal is read with `useSyncExternalStore`, for the React 19 lint rules; same behaviour.
- **Registry.** Our destinations; `match.nested` added so Chat owns `/app` and `/app/c/…` without lighting up on `/app/history`; island routes dropped. Masayume's registry tests ported (`tests/shell/nav-items.test.ts`).
- **AppStrip.** Two lines of ours, link "How it works" → `/#how`; the lead glyph is a checked box (theirs is a phone because their strip is about installing).
- **Marquee.** Their prices/next-close → KeeperHub's live network list plus, signed in, this org's latest receipts from the new `GET /api/ledger` (token-free projection; Activity/History reuse it). Added an UNAVAILABLE state when the network list fails, so a dead feed does not read as LOADING forever.
- **Money pill.** Native token on the selected network to 4 places (KeeperHub reports per chain; there is no price to sum by), em dash until it answers. Its `+` fires `keeperhub:open-funds`; nothing listens until the fund modal (slice 4).
- **Account pill.** Portaldot's Identicon replaces Masayume's gradient dot. Signed out, "Connect KeeperHub" goes straight to `/api/auth/login` (the modal is slice 4). No menu behind it yet (slice 4). Masayume's "Wrong network" outline slot is used for "KeeperHub unreachable" (click retries).
- **Network pill (additive).** Money-pill geometry, `ui/menu` radio list grouped Mainnets / Testnets from the live list, cookie `kh_network`, Base Sepolia (84532) until picked. `ChainMark` takes the platform's `name` so networks `lib/chains` does not know (e.g. 0G) get a letter disc instead of "?".
- **Chat route (first change to v1's routes).** `instructionsFor()` adds one line naming the selected network, on normal turns and on approval resumes. Test added.
- **Small shared changes.** `ui/sheet` takes `overlayClassName` (Masayume's sheet does); `ui/menu` gains radio group/item/indicator; Portaldot's diamond moved to `ui/diamond-mark.tsx`, which the landing nav and footer now import (no visual change); `formatDecimalString` in `lib/format.ts`.
- **Placeholders.** `/app`, History, Activity and Automations render Masayume's `CapabilityPending` (with a side gutter added). `/docs`, `/docs/actions`, `/docs/mcp` still 404 until slice 7 — see the open question in `DECISIONS.md`.

## 4 · Sign-in & onboarding
- [x] Connect ladder (Masayume `ConnectButton.tsx` states) with "Connect KeeperHub".
- [x] Sign-in modal: Portaldot `wallet-picker.tsx` shell + Masayume modal grammar; one option "Continue with KeeperHub" → `/api/auth/login`.
- [x] Portaldot `wallet-gate.tsx` identity card on `/app` when signed out.
- [x] Draft through OAuth: stash prompt in sessionStorage before redirect; OAuth callback lands on `/app`. *(The auto-send on arrival needs the chat, so it moved to slice 5's first item.)*
- [x] Account menu (Masayume `HeaderAccount.tsx`): org wallet balance, copy address, explorer link, spending limits, Disconnect.
- [x] Tutorial (Masayume `features/onboarding/*`), 5 steps, last step embeds real Connect, auto-close on connect.
- [x] Fund-your-wallet modal (Masayume `AddFunds.tsx` + Portaldot `address-qr.tsx`).
- [x] First-receipt welcome (Masayume `CreditWelcome.tsx`); recovery on return (Masayume `WriteRecovery.tsx` + `readSettledReceipt`).
- [ ] Abu reviews `/app` signed out and `/dev/onboarding` (every surface opens from there), desktop + ~390px; then signs in once to see the account menu.

**Slice 4 report (2026-09-12).** Typecheck, lint, build clean. `pnpm test`: 442 passing in 38 files (+21).
Checked against the running dev server: `/app`, `/app/history`, `/dev/onboarding`, `/` return 200; `/api/account/limits` and
`POST /api/ledger/recover` 401 signed out; `/api/auth/callback?error=access_denied` → `/app?connect=denied`, a callback with no
transaction → `/app?connect=failed`; logout → `/app`. Nothing has been looked at in a browser and nothing signed-in has been exercised.
Files: `components/shell/sign-in/{sign-in,connect-button,sign-in-gate,pending-draft}.tsx|ts`, `components/shell/header/{account-menu,use-floating-menus}.ts*`,
`components/shell/onboarding/{tutorial.tsx,steps.ts,use-first-run.ts}`, `components/shell/funding/{add-funds,receipt-welcome}.tsx`, `receipt-announce.ts`,
`components/shell/write-recovery.tsx`, `components/data/address-qr.tsx`, `lib/session/spend-cap.ts`, `app/api/account/limits`, `app/api/ledger/recover`,
`app/(app)/dev/onboarding` + `components/dev/onboarding-review.tsx`.
Differences from the references, all deliberate:
- **Engine change (second to v1's lib).** A write's KeeperHub execution id is now saved on its intent row the moment the broadcast returns, before the receipt poll (`setIntentExecutionId` in `lib/data/ledger`, `recordExecutionId` in `lib/ledger`, called at both broadcast sites in `lib/execution`; best-effort, never throws). Before, the id was saved only with the final state, so a tab closed mid-poll left nothing to recover with. `recoverOpenWrites` in `lib/execution`: intents older than 2 min, 10 per visit; with an id it asks `get_direct_execution_status` once and closes on `readSettledReceipt`; without one it waits out KeeperHub's 24 h idempotency window, then closes as `expired`. Never re-sends. Tests: `tests/execution/recover.test.ts`, `tests/ledger/execution-id.test.ts`.
- **Connect ladder.** Masayume's "Wrong network" rung → "KeeperHub unreachable" (retries). Signed-in rung is a label, not a button: no account modal exists; the menu lives in the header. `onBeforeOpen` lets the tutorial and Add funds close before the sign-in modal opens, so two modal dialogs never stack.
- **Sign-in modal.** One row, "Continue with KeeperHub" with an OAuth tag; no detection loop or install rows. The row's status line is KeeperHub's reachability from the live network list, where Portaldot shows "detected". Footer: "Org wallet signs · no browser wallet" / "mcp:read · mcp:write". Its error box shows why a return from KeeperHub failed (`?connect=denied|failed`, stripped from the URL).
- **Draft.** sessionStorage, not the URL (OAuth drops the URL); 10-minute window matching the OAuth transaction cookie; `takeDraft` hands it over once. Callback success, cancel, failure and logout now land on `/app` (were `/`).
- **Identity card.** Copy says what signs here; pill opens sign-in (KeyRound icon for Portaldot's Wallet); footer figures are real (442 actions · 34 integrations from the registry, live network count) where Portaldot hard-coded them; unreachable shows a retry pill. While the session is unknown `/app` holds an empty block of the card's height.
- **Account menu.** Rows: org wallet on the selected network; "Spent today" = KeeperHub's `GET /api/analytics/spend-cap` (OAuth, mcp:read; EVM pair only, read when the menu opens). Portfolio → Activity. Portaldot WalletPill's Copy address and View on explorer added. Closes on route change too. Disconnect posts the logout form. Masayume's vermilion Disconnect → primary (Portaldot's own Disconnect is destructive — open question in `DECISIONS.md`).
- **Tutorial.** Pressing Connect on the last step marks it seen and closes it (connecting leaves the page); arriving on the last step signed in closes it too. `tutorial.css` → utilities, keyframe in `shell.css`. Buttons 48px as in Masayume. All five screens' copy is ours.
- **Add funds.** KeeperHub has no faucet: the modal is the org wallet address (copy) + Portaldot's QR + a balances grid (native + up to 3 tokens) + one CTA ("Check balance again" until something is held, then "Start in chat →", Masayume's done state). No external faucet links (open question). Frame is `ui/dialog`, which carries the same modal grammar as `.fund-modal`.
- **First-receipt welcome.** Event `keeperhub:receipt`, one-time per org via localStorage. Fired today only by recovery; the write card fires it in slice 6. Copy ours.
- **Recovery.** Runs server-side (record + token live there); client toasts per outcome, one for everything still unanswered. Asked once per org per page load and not cancelled on unmount, so dev double-runs cannot swallow the answer (Masayume's cancel-on-unmount would).
- **Small shared changes.** `IdentityState` signed-in carries `orgId`; `signedOutRedirect(request, reason)`; Header mounts AddFunds + ReceiptWelcome (as Masayume's does); layout mounts `SignInProvider` + `WriteRecovery`.
- **Review page** `/dev/onboarding` (noindex, unlinked): opens sign-in, tutorial, Add funds, and previews the welcome and the four recovery toasts under a "preview" org so the real welcome is untouched.

## 5 · Chat surface
Sources: Portaldot `components/app/{chat-app,markdown}.tsx` · DeepBookie `components/chat/{ChatWorkspace,Chat,MessageList,MessagePart,ChatSessionDropdown,ArchivedSession}.tsx`, `chatHome/*`, `app/globals.css:101-171` · v1 `components/chat/{Conversation,Composer,ThreadSwitcher,StarterSuggestions}.tsx`, `app/c/[conversationId]/page.tsx`, `lib/registry/surface-suggestions.ts` · 21st `_notes/21st/21st-3035.txt`.
- [x] **5.1 Pages.** `app/(app)/app/page.tsx` (new chat) and `app/(app)/app/c/[conversationId]/page.tsx` (v1 server load: session → `getConversation` → `validateUIMessages`, `notFound`, honest `loadNotice`; signed out renders the gate and never loads). Both render `components/chat/chat-workspace.tsx` (DeepBookie ChatWorkspace: dropdown strip above the chat), keyed by conversation. Full-height column under the fixed chrome; composer clears the phone pill. Port v1 `tests/conversations/load.test.ts`.
- [x] **5.2 Chat engine.** `components/chat/chat.tsx`: v1 `Conversation` (module transport sends only `{conversationId, message}`; create-then-send with the re-entrancy latch; `history.replaceState` to `/app/c/[id]`; approval auto-resume via `sendAutomaticallyWhen`; the card callbacks `ceremony` + `rerun` move to slice 6) inside DeepBookie `Chat` behaviour: thinking dots on `submitted`, autoscroll only within 120px, error banner + Retry (`regenerate`; same message id, so the transcript merge replaces rather than duplicates), composer locked while a part is `approval-requested` ("Approve or cancel the action above to continue."), a lapsed session opens the sign-in modal carrying the unsent text.
- [x] **5.3 Old conversations.** Abu, 2026-09-13: "continue if recent". One 30-minute rule (DeepBookie `INACTIVITY_MS`): a conversation last updated within 30 min opens live; older opens read-only (DeepBookie Chat.tsx:196-209 bar in place of the composer: "This conversation was archived. It's now read-only." + New chat); a live chat idle 30 min turns read-only in place. The chat route stays as is (v1's server-side approval checks still guard a stale click).
- [x] **5.4 Messages.** Portaldot user bubble (+ `whitespace-pre-wrap`, the composer is multi-line) and `components/chat/markdown.tsx` verbatim; key by id+index (DeepBookie). Until slice 6, each tool part is one honest mono line (tool name + state).
- [x] **5.5 Arrival.** One draft, first found wins: `takeDraft(sessionStorage)` → `?q=` → `?prompt=`. Signed in: send once, strip the URL (Portaldot :112-124, DeepBookie :94-110). Signed out: prefill the composer and open sign-in (DeepBookie :104-107); sending signed out also opens sign-in with the text.
- [x] **5.6 Composer.** `components/chat/composer.tsx`: 21st PromptBox on Base UI — textarea auto-grows to 200px, Enter sends / Shift+Enter newline, text restored when a send fails (v1). Plus-menu (`ui/menu`): network radio list (shared with the header pill), "Browse all actions" → `/docs/actions`. Mic present but disabled with "Voice isn't connected yet" until slice 9. Image attach dropped (nothing to attach to). Paint: Portaldot hero pill (card, border turns primary on focus), primary send.
- [x] **5.7 Launcher.** `components/chat/home/*`: DeepBookie ChatHome (greeting pill, desktop rail of 246×188 cards with edge fades + `animate-rise` stagger, mobile hero + 2-col grid), CategoryCard/Tile, motifs, tile icons. Categories are ours: `STARTER_SUGGESTIONS` (Chainlink, Lido, Rocket Pool, Sky) + Portaldot "Ask about you" (org wallet, send to myself) + discover actions + History (link). Motifs show shapes and only real figures (action/integration count, network count, conversation count, the org wallet address) — DeepBookie's decorative 62% and $1,284.10 are not copied. DeepBookie keyframes (`live-pulse`, `pulse-up`, `spark-dot`, `breathe`, `drift`) into `shell.css`. Signed out never shows the launcher (the identity card stands there), so the "Connect wallet to use" card state is not ported.
- [x] **5.8 Session dropdown.** `components/chat/session-dropdown.tsx`: DeepBookie anatomy (chat glyph + title + rotating caret; "+ New chat"; CONVERSATIONS label; title + mono time rows; skeleton; "No past chats yet.") on a Base UI Popover; list fetched fresh on open (v1); v1 inline rename and delete per row; deleting the open one returns to `/app`.
- [x] **5.9 Checks.** Unit tests for the pure rules (pending-approval lock, archive rule, draft precedence, launcher prompts anchored to real registry reads). Typecheck, lint, build, tests. Walk the routes.
- [ ] Abu restarts `pnpm dev` (see report), then reviews `/app` signed in (launcher, a read, a follow-up, New chat, dropdown rename/delete, a reopened conversation) and signed out, desktop + ~390px.

**Slice 5 report (2026-09-13).** Typecheck, lint, build clean. `pnpm test`: 463 passing in 41 files (+21: `tests/chat/{chat-rules,launcher-categories}.test.ts`, `tests/conversations/load.test.ts` ported from v1).
Checked on a fresh production server (`next start -p 3002`, then stopped): `/app`, `/app?prompt=hi`, `/app/c/<unknown>` return 200 signed out; `GET`/`POST /api/conversations`, `PATCH /api/conversations/x`, `POST /api/chat` 401. **Abu's dev server (up since 2026-09-12 19:07) answers 404 for `/api/chat` and `/api/conversations/**`**: it never registered those route files (added after it started, first requested today; touching them did not help). Restart `pnpm dev` before reviewing. Nothing looked at in a browser; nothing signed-in exercised.
Files: `components/chat/{chat,chat-workspace,chat-rules,composer,markdown,message-list,session-dropdown}.ts*`, `components/chat/home/{chat-home,categories,category-card,category-tile,motifs,tile-icons}.ts*`, `app/(app)/app/page.tsx`, `app/(app)/app/c/[conversationId]/page.tsx`, launcher keyframes in `app/(app)/shell.css`.
Differences from the references, all deliberate:
- **Layout.** The column fills the height under the fixed header (`-mb` cancels the shell's bottom pad); on phones the composer strip pads above the floating pill. DeepBookie's dropdown strip sits above the chat, signed in only.
- **Engine.** v1 `Conversation` without the Inspector placeholder, its empty state (→ launcher) and the card callbacks (slice 6). DeepBookie's client-side transcript PATCH and outcome POST are not ported: the server stores the transcript (v1) and the ledger lives in `lib/`.
- **Tool parts.** One dashed mono line per part ("SEARCH_ACTIONS · DONE · card not connected yet"). Consequence until slice 6: a write that stops for approval locks the composer with nothing to click; New chat is the way out.
- **Archive.** One bottom bar for both the idle chat and a reopened old one; DeepBookie ArchivedSession's top header is not used (the dropdown strip is there). A reopened conversation starts at the top of the scroll, as DeepBookie's replay does.
- **Retry.** `regenerate` re-sends the same user message id and the transcript merge replaces it. Known edge: if a turn failed mid-stream after the server stored a partial reply, that partial reply stays stored. A lapsed session shows "Your KeeperHub session ended." + Connect KeeperHub (v1) instead of Retry.
- **Arrival.** Signed out from a link (`?q=`/`?prompt=`): prefill + open sign-in. Signed out with a sign-in draft (sign-in did not finish): prefill only, so the modal's return error is not wiped.
- **Composer.** 21st image attach and "Tools" popover dropped; the plus opens network (the header pill's list, now shared as `NetworkMenuGroups`) + "Browse all 442 actions" (404 until slice 7); the active-tool chip shows the selected network in telemetry cyan; mic is `aria-disabled` with a tooltip; 16px text (21st default, avoids phone zoom); violet round send; Portaldot hero-pill glass with a primary focus border.
- **Launcher.** DeepBookie's anatomy with 8 cards of ours (Prices · Your org wallet · Send to yourself · Liquid staking · Rocket Pool · Savings vault · Discover actions · History). Registry cards take v1's drift-guarded starter prompts and disappear if one drifts. No invented figures: no LIVE badges, no count-up, no "62%"; the only numbers are the org wallet, 442 actions, your conversation count and the live network count in the greeting pill. Portaldot's `animate-rise` (0.22s, 6px) replaces DeepBookie's same-named rise (0.55s, 12px), keeping DeepBookie's stagger. Family labels mono; DeepBookie ink → `border-strong` on hover, prompt strip inverts to primary. The "Connect wallet to use" card state is not ported (signed out shows the identity card).
- **Dropdown.** Base UI Popover in the `ui/menu` panel paint and motion; fetched on open (DeepBookie polls every 4s); v1's inline rename and delete per row; times in UTC (Masayume) instead of DeepBookie's local "Jun 20, 4:15 PM". The pill's title refreshes 1.5s after the first turn, when the route has named the conversation.
- **Pages.** `/app/c/[id]` is v1's loader; signed out it renders the identity card without loading, and a failed session read is logged and shown as signed out. `ChatGate` removed (the chat decides); `SignInGate` exported. The first-run tutorial stays on `/app` only.
- **Copy.** Greeting, card titles and descriptions, the hint lines, the archive bar and the tool-part line are the agent's wording.

## 6 · Tool calls & cards
Planned with Abu 2026-09-13 (decisions 11–14). Two parts, Abu reviews after each.
Sources: Portaldot `components/cards/receipt-card.tsx`, `components/tools.tsx` · DeepBookie `components/widgets/{SignReceipt,ReceiptController,receiptState}.tsx|ts`, `spot/SwapCard.tsx`, `ui/Sparkline.tsx`, `VaultPoolCard.tsx`, `chat/{MessagePart,StaticToolReceipt}.tsx` · v1 `components/cards/**`, `components/chat/Conversation.tsx` · 21st `_notes/21st/tool-calls-section.txt`, `21st-12423.txt` · KeeperHub fork `protocols/chainlink.ts:650-740`, `public/protocols/*.png` @ `946eeb5c9`.

**Part A: frame, tools line, read cards**
- [x] **6.1 Card logic.** v1 `select-renderer`, `card-state`, `write-card`, `editable`, `fields/field-widget`, `format`, `money`, `money-mover` into `components/cards/` unchanged, with v1 `tests/cards/*`.
- [x] **6.2 Receipt frame.** Portaldot `receipt-card.tsx` verbatim (`motion/react`); `tools.tsx` helpers into `components/cards/parts.tsx`. Chip treatment → tone (accent → pending, heavy → destructive, receipt → success); the state label sits in the meta slot.
- [x] **6.3 Renderer + chat wiring.** v1 `CardRenderer` + `CardErrorBoundary` + `DegradedStub` repainted. `chat.tsx` gains v1's `ceremony` + `rerun` (stable refs), superseded set, live-replacement focus. `readOnly` (archived) passes no callbacks and skips live fetches.
- [x] **6.4 Tools line.** heygaia ToolCallsSection anatomy + tool-group shimmer/elapsed while running. `groupParts()` in `chat-rules`: consecutive tool parts (step markers ignored) → one line, then that run's cards.
- [x] **6.5 Logos.** KeeperHub `public/protocols/*.png` → `public/protocols/` (23 of 34 integrations; aave serves both versions, frax-ether → frax-ether-v2, weth → wrapped). `components/data/integration-mark.tsx`, letter disc for the other 11.
- [x] **6.6 Lookup card rule.** `lookupIsAnswer()`: a `search_actions` card only when no tool part follows it and a text part follows or the turn is over. Capability card in Portaldot list grammar.
- [x] **6.7 Read cards.** Skeleton; Generic result (v1 editable read card, Portaldot GenericResultCard values); Balance (`web3/check-balance`, `web3/check-token-balance`: Portaldot BalanceCard, ramp from raw units, QR, "ORG WALLET" when it's yours); Needs credential; Error (VOID only for failures, boundaries get the pending tone).
- [x] **6.8 Price card + chart.** 8 named `chainlink/*-latest-round-data` feeds; `lib/price-feeds.ts` (addresses from KeeperHub, decimals confirmed live); Portaldot BlockInfoCard grammar + DeepBookie Sparkline + "last 12 updates ±%"; expand to full screen. `POST /api/cards/price-history`: decimals + up to 11 `get-round-data` reads, stop at a gap, in-memory cache. Engine change: `recordRead: false` on `routeToolCall` so chart lookups stay out of the ledger.
- [x] **Stop A.** Checks clean; Abu reviews signed in (price question, "what can you do with Aave?", org wallet balance, a failing read, an archived chat), desktop + ~390px. *(Abu, 2026-09-13: "lets continue with building", then after trying the app: "its works and its cool stuff lets continue".)*

**Slice 6 part A report (2026-09-13).** Typecheck, lint, build clean. `pnpm test`: 632 passing in 53 files (+169: v1's eight `tests/cards/*` files (136), new `tests/cards/{read-results,price-feeds,tool-meta}.test.ts`, `tests/chat/tool-runs.test.ts`, one engine test in `tests/execution/route.test.ts`).
Checked on a fresh production server (`next start -p 3002`, then stopped): `/app` and `/app/c/<unknown>` 200; `POST /api/cards/price-history`, `/api/chat`, `/api/chat/simulate` 401 signed out; `/protocols/chainlink.png` 200. Abu's `pnpm dev` still needs a restart (slice 5). Nothing looked at in a browser and nothing signed in exercised. **In particular no real KeeperHub read has reached the new cards:** the price and balance cards read KeeperHub's result shape as found in its source (`FINDINGS.md`, "KeeperHub read results"); if the live shape differs, those reads fall back to the generic result card.
Files: `components/cards/{receipt-card,parts,skeleton-card,card-error-boundary,error-card,needs-credential-card,capability-card,read-card,balance-card,price-card,sparkline,card-renderer}.tsx`, `components/cards/tool-meta.ts`, `components/cards/fields/{field-editor,option-switch}.tsx`, the eight v1 logic files, `components/chat/tool-timeline.tsx`, `components/data/integration-mark.tsx`, `lib/{read-results,price-feeds}.ts`, `app/api/cards/price-history/route.ts`, `public/protocols/*.png` (22), and changes to `components/chat/{chat,message-list,chat-rules}.ts*`, `app/(app)/shell.css`, `lib/execution/index.ts`.
Differences from the references, all deliberate:
- **Frame.** `receipt-card.tsx` verbatim (`motion/react`). The meta strip's left side is the operation id (Portaldot shows its tool id there); the registry label is the card's first line (v1's header title). The meta slot always carries a state (READ, EDITED, FETCHING, VOID, ROUND n…), so Portaldot's ticking ISSUED time never shows.
- **Tones.** v1 chip treatments map to Portaldot tones; an edited read card is pending. A boundary (not available, paused, needs access, check inputs, reconnect) is pending with no stamp; only a failure is VOID.
- **Tools line.** heygaia anatomy with zinc → tokens; "Used N tools", or "Using N tools" with tool-group's shimmer (keyframes in `shell.css`) and elapsed time only while running, since durations are not stored. Details are key/value lines instead of heygaia's CompactMarkdown; a failed call reads destructive (tool-group's `isError`). One line per run of consecutive calls. Row titles are ours ("Looked up actions for …", the registry label, "Called fn").
- **Logos.** 22 KeeperHub files cover 23 integrations; the other 11 get a letter disc; the catalog lookup uses a search glyph.
- **Lookup card.** v1 CapabilityList data in Portaldot's list grammar with tool-grid's pending "signs" pill for actions that change something.
- **Generic read.** v1 behaviour (seed once, Run again, alternatives, retired note). KeeperHub's `{ success, result, addressLink }` is unwrapped into named rows with a "contract" explorer link. "Run again" is Portaldot's ghost pill, because v1 never gives a read the accent. Field rows use Portaldot's mono label and hero-pill input; option pills are tool-grid's category pills.
- **Balance.** Portaldot BalanceCard minus Free/Reserved. A native balance uses 18 decimals on EVM networks and 9 on Solana; a network `lib/chains` does not know falls back to the generic card. The meta slot shows ORG WALLET for your own address, else the network.
- **Price.** BlockInfoCard grammar; meta slot is the round within its phase; DeepBookie Sparkline in telemetry cyan with VaultPoolCard's footer; Portaldot's "gathering signal" while loading, "history unavailable" on failure. Expand opens `ui/dialog` widened to 3xl (still a bottom sheet on phones) with a 220px chart and every update listed. A read-only chat shows the price without a chart. Places: 2 for prices ≥ 100, 4 below, 6 for ETH-quoted pairs.
- **History route.** One decimals read plus up to 11 rounds in parallel; stops at a missing round or a phase start; caches up to 1,000 entries; falls back to Chainlink's convention if the decimals read fails; a thrown error answers 502.
- **Engine change (third to v1's lib).** `recordRead` on `routeToolCall`, default true; only the history route passes false.
- **Chat.** `chat.tsx` gained v1's `rerun` door (blocked while busy, awaiting approval, or archived) and passes `readOnly` when archived. The write `ceremony` callbacks moved to part B with the write card. Until then a write shows a NOT CONNECTED frame telling you to start a new chat.
- **Copy.** Card first lines for non-registry tools, the tools-line titles, "history unavailable", the NOT CONNECTED notice and the balance meta labels are the agent's wording.

**Part B: the transfer card**
- [x] **6.9 Write card.** Portaldot frame around DeepBookie SignReceipt anatomy: kicker, status line, doc number `KH·XXXX·XXXX` = hash of tool + canonical input, rows from money-mover/requestRows, dry-run inset (+ revert acknowledgement), CTA ladder ending on **Authorize**, ref guard against double clicks, rows + preview frozen at click, executing / not sent / EXECUTED stamp + explorer link + `announceReceipt` / DeepBookie cancelled state / "Never authorized" read-only.
- [x] **6.10 Edit everything but the action.** Card: transfer amount, recipient, network (live platform list), token; protocol write params (actionType fixed); contract args + value (contract, function, mutability fixed); approve spender + allowance. Edit → EDITED, new doc number, dry run again. Server: `validateSameOpEdit` → `validateEditedWrite` (same tool + identity, edited input passes the tool's schema), re-sign as today; `tests/chat/route.test.ts` updated.
- [x] **6.11 Swaps.** KeeperHub `/api/execute/swap` answers 501. Trace whether any swap action reaches it; if so, `isWriteUnavailable` + honest card; else note it in `FINDINGS.md`. *(Nothing reaches it; noted.)*
- [x] **6.12 Checks.** Tests for `groupParts`, `lookupIsAnswer`, doc number, feed table, history stop-at-gap, `recordRead:false`, edit validation. Typecheck, lint, build, tests; routes on a fresh `next start`.
- [x] **Stop B.** *(Abu, 2026-09-13: "its works and its cool stuff lets continue". Card wording kept as built.)* Abu restarts `pnpm dev`, then reviews signed in: Send to yourself → edit amount + network → dry run → Authorize → EXECUTED + explorer + welcome; a Cancel, a double click, a reload mid-send; desktop + ~390px.

**Slice 6 part B report (2026-09-13).** Typecheck, lint, build clean. `pnpm test`: 665 passing in 56 files (+33: new `tests/cards/write-form.test.ts`, `tests/registry/edited-write.test.ts`, `tests/cards/price-history.test.ts`; `tests/chat/chat-rules.test.ts` and `tests/chat/route.test.ts` updated). `groupParts`, `lookupIsAnswer`, the feed table and `recordRead:false` were already covered in part A.
Checked on a fresh production server (`next start -p 3002`, then stopped): `/app`, `/app/c/<unknown>`, `/protocols/lido.png` 200; `POST /api/chat`, `/api/chat/simulate`, `/api/cards/price-history` 401 signed out. Nothing looked at in a browser, nothing signed in exercised, **no real transfer sent**: the dry-run facts read KeeperHub's simulate result as found in its source (`FINDINGS.md`).
Files: `components/cards/{write-card-view,write-card-parts}.tsx`, `components/cards/write-form.ts`, `components/cards/fields/network-field.tsx`, `lib/registry/edited-write.ts`, `lib/canonical-json.ts`; changes to `components/cards/{write-card.ts,card-renderer.tsx}`, `components/chat/{chat,message-list,chat-rules}.ts*`, `components/shell/header/network-pill.tsx`, `app/api/chat/{route,approval-signature}.ts`, `app/api/cards/price-history/route.ts`, `lib/price-feeds.ts`, `docs/FINDINGS.md`.
Differences from the references, all deliberate:
- **One card, whole life.** Every write stage draws `WriteCard` in the same place, so the dry run seen at the click stays on the sending and executed views (DeepBookie SwapCard's snapshot). Frame, from → to + QR, amount inset, Authorize/Cancel pills and tx inset are Portaldot TransferCard; kicker, status line, document number, dimmed body + spinner while sending and the cancelled state are DeepBookie SignReceipt; the dry run, revert acknowledgement and "not sent" + Try again are v1 WriteCard.
- **Stamps and labels.** EXECUTED (success) once KeeperHub's verified receipt lands, where Portaldot stamps PAID at FINALIZED. Meta slot: AWAITING AUTHORIZATION → EDITED → AUTHORIZING → SENDING (or CANCELLING, NOT SENT) → EXECUTED / CANCELLED. A write failure stays the part A error card (VOID). Read-only chat: NEVER AUTHORIZED, or NO RECEIPT when it was authorized but the receipt never reached the chat.
- **Document number.** `KH·XXXX·XXXX` = FNV-1a over the tool + canonical input (DeepBookie hashes the call id). Any edit prints a new one. Display only, not a security value.
- **CTA ladder.** Running dry run… → Dry run failed → Acknowledge the warning → Authorize (Authorizing… after the click); a ref blocks a second click in the same tick (DeepBookie ReceiptController).
- **Editing (decision 11).** Edit opens the fields; applying runs the edit check, prints a new document number and re-runs the dry run; Authorize is hidden while editing. Transfer: amount, recipient, network (KeeperHub's live list), token. Approve: spender + allowance. Other contract calls: arguments, and value when payable. Protocol action: its own registry fields; a chain-select without options uses the network menu narrowed to the action's networks. Server: `validateSameOpEdit` is replaced by `checkEditedWrite` (`lib/registry/edited-write.ts`), shared with the card: same action, fixed parts unchanged (contract, network, function, mutability, ABI, gas for calls; unlisted parameters for protocol actions), the tool's schema, plus shape checks the loose schemas lack (plain decimal amounts, EVM or base58 addresses by network, JSON argument list, whole-number allowance). Rejection message reworded.
- **Underscore fields.** v1 hid every `_` parameter; `lido/wrap`'s amount is `_stETHAmount`. Rows and the edit check now go by the action's field list.
- **Chat.** `chat.tsx` gains v1's ceremony (approve carrying an edited proposal via `withEditedInput`, cancel, try again, dry run). When an answer's post fails, the banner offers no Retry (regenerating would drop the answer), says to use Try again on the card, and the composer stays locked. The composer hint says "Authorize or cancel". A landed receipt fires `announceReceipt` (first per org → welcome) and re-reads the money pill, only when the card watched it land.
- **Small shared changes.** `canonicalJSON` moved to `lib/canonical-json.ts` (the approval signature re-exports it); `NetworkMenuGroups` takes an optional value, setter and allowed ids; the history route's stop-at-gap is `unbrokenRun` in `lib/price-feeds.ts`.
- **Known behaviour.** A reload while a transfer is sending shows the proposal again (the server stores the answer only when the resumed turn ends). Authorizing again re-sends under the same idempotency key, so KeeperHub replays the first execution rather than sending twice (24 h window), and write recovery closes the record.
- **Copy.** The kicker names, status lines, button ladder, dry-run lines, the acknowledgement, the cancelled/never-authorized/no-receipt notes and the edit rejection are the agent's wording.

## 7 · Pages
Planned with Abu 2026-09-13 (decisions 15–18). Full plan: `~/.claude/plans/soft-moseying-kazoo.md`.
Sources: DeepBookie `app/(app)/{history,positions,markets,markets/[id]}/page.tsx`, `components/shell/Page.tsx`, `components/widgets/{PositionsTable,MarketsBoard,TradeTape}.tsx` · Portaldot `app/docs/*`, `components/docs/{docs-nav,tool-grid,skill-content}.tsx`, `components/ui/code-block.tsx` · KeeperHub fork `lib/mcp/tools.ts` (`list_workflows`, `get_workflow`, `execute_workflow`, `get_execution`), `app/api/workflows/[workflowId]/{executions,simulate}`.
- [x] **7.0 Receipt poll fix.** KeeperHub's `get_direct_execution_status` takes `execution_id`; `lib/execution` sent `executionId` (receipt poll + recovery), so a sent write's status read failed and it was recorded as a failure. Fix both, assert the args in tests, note in `FINDINGS.md`.
- [x] **7.1 History.** DeepBookie journal gallery; cards open `/app/c/[id]` (decision 16). `listConversationSummaries` (executed count per conversation, cap 60) behind `GET /api/conversations?summary=1`. Shared page chrome `components/pages/page.tsx` (DeepBookie `Page`/`PageHeader`).
- [x] **7.2 Activity.** DeepBookie table (lg+) / cards; Actions · Reads · All pills, starting on Actions (decision 17). `GET /api/ledger?kind=` + `network` and `label` per row; Load more by `before`.
- [x] **7.4 Docs.** Routes under `app/(app)/docs` in the app header (decision 15): Portaldot docs layout + DocsNav; Getting started (InstallCommand extracted from the landing, CodeBlock port); `/docs/actions` ToolGrid over the registry (integration pills, signs pill, copyable prompt); `/docs/mcp`.
- [x] **7.3 Automations.** `lib/automations` over `list_workflows` / `get_workflow` / per-workflow executions REST / simulate / `execute_workflow` + `get_execution`; ledger intent → terminal for runs, recovery learns workflow runs. `/app/automations` board (All · Live · Off), `/app/automations/[id]` detail with step timeline, runs tape and **Run now** behind a confirm card (decision 18).
- [x] **7.5 Checks.** Tests (poll args, summaries grouping, ledger kind/projection, workflow trigger/step helpers, run status → ledger state, catalog filtering + prompts, nav routes resolve). Typecheck, lint, build, tests. Restart the dev server myself, curl the six pages (200) and new APIs (401).
- [ ] **Stop.** Abu reviews signed in, desktop + ~390px: History opens the right chat; Activity pills + a sent transfer EXECUTED with explorer (confirms 7.0); an automation's detail + Run now; docs menus and search.

**Slice 7 report (2026-09-13).** Typecheck, lint, build clean. `pnpm test`: 689 passing in 59 files (+24 since part B: `tests/pages/{activity,docs-catalog}.test.ts`, `tests/automations/shape.test.ts`, `tests/cards/price-history.test.ts` from part B, and additions to `tests/execution/{route,recover}.test.ts`, `tests/ledger/route.test.ts`, `tests/conversations/routes.test.ts`). Dev server restarted fresh on 3001 (`.next` cleared): `/app/history`, `/app/activity`, `/app/automations`, `/app/automations/<id>`, `/docs`, `/docs/actions`, `/docs/mcp` → 200; the five automations APIs, `/api/ledger?kind=reads` and `/api/conversations?summary=1` → 401 signed out. Nothing looked at in a browser, nothing signed in exercised, **no automation run and no transfer sent**: KeeperHub's workflow, execution and simulation shapes are read from its source (`FINDINGS.md`).
Files: `components/pages/{page,history,activity,automations,automation-detail,automation-parts}.tsx`, `components/docs/{docs-nav,action-grid,install-command,copy-row}.tsx`, `components/docs/catalog-filter.ts`, `components/ui/code-block.tsx`, `lib/{activity,docs-catalog}.ts`, `lib/automations/{index,shape}.ts`, `app/(app)/docs/{layout,page}.tsx`, `app/(app)/docs/{actions,mcp}/page.tsx`, `app/(app)/app/{history,activity,automations}/page.tsx`, `app/(app)/app/automations/[workflowId]/page.tsx`, `app/api/automations/**`; changes to `lib/execution/index.ts`, `lib/ledger/index.ts`, `lib/data/{index,ledger}.ts`, `app/api/{ledger,conversations}/route.ts`, `components/landing/how-install.tsx`, `components/cards/write-card-parts.tsx`.
Differences from the references, all deliberate:
- **Engine (fourth and fifth changes to v1's lib).** 7.0: the receipt poll and recovery send `execution_id` (they sent `executionId`, so no sent write could ever be confirmed). Automation runs: `writeRunIntent` + `readLedgerRow` in `lib/ledger`; `startWorkflowRun` (intent row, then `execute_workflow` with a fresh idempotency key, execution id stamped), `settleWorkflowRun` (terminal only from `get_execution`) and a recovery branch for rows with a workflow id in `lib/execution`. A run KeeperHub accepted without an execution id stays open rather than being called a failure. `listLedgerRows` takes `kind`; `listConversationSummaries` counts receipts per conversation.
- **Page chrome.** DeepBookie `Page`/`PageHeader`/centred states in Portaldot tokens (`components/pages/page.tsx`); the shell's main scrolls, and the header wraps on phones so its pills drop below the title. Filter pills use Portaldot tool-grid's pill grammar.
- **History.** DeepBookie gallery; a card opens `/app/c/[id]` (decision 16) instead of an in-page replay; Portaldot's 1px perforation; UTC times; "reads only" for DeepBookie's "read-only" pill (read-only already means archived here); executed count from ledger receipts; loaded once (DeepBookie polls 4 s); newest 60 with a transcript.
- **Activity.** DeepBookie PositionsTable split (table lg+, cards below) over ledger rows: what ran (integration mark, label, op id), network, outcome in the write card's words, transaction + explorer, UTC time, a link back to the chat or automation. Actions / Reads / All (decision 17). Load more by cursor (DeepBookie has no paging). `/api/ledger` now returns `label`, `integration`, `network` and takes `?kind=`.
- **Docs.** Under the app header (decision 15); DocsNav and the action grid's sticky bar sit below it. Getting started keeps Portaldot's steps, Configuration block and catalog CTA, lists the variables the copilot really reads, and moves the install terminal to MCP setup (this app is used in the browser, not installed). Actions: the registry grouped by integration (34 pills, section marks, descriptions), "signs" for anything but a read, system and quarantined actions left out; the 8 curated starter prompts are copied verbatim and every other action copies "Use <label>." MCP setup: the install terminal with Claude Code and Cursor, three copy rows; no collapsible skill file. `InstallCommand` moved out of the landing into `components/docs/install-command.tsx` with its clients as a prop (landing unchanged); `CodeBlock` port with one lint adapter.
- **Automations board.** DeepBookie MarketsBoard + filters + sort. KeeperHub's enabled switch only matters for triggers that fire on their own, so the states are Live, On demand (manual), Off, Deactivated and the filters All / Live / On demand / Off. No last-run column (KeeperHub's list carries no runs; one request per row). Empty state links to KeeperHub, since creating from chat is slice 8.
- **Automation detail.** DeepBookie market detail: back link, title + status + Run now, stats card, then steps (in edge order, disabled steps struck through) and recent runs in TradeTape grammar (status, trigger source, UTC start, duration, first tx hash + explorer). Runs history unavailable is said plainly.
- **Run now (decision 18).** Masayume dialog holding the write card's anatomy (kicker "Automation run", status line, document number over workflow id + last update, what runs, a plain warning that value-moving steps will not stop again). The dry run is KeeperHub's advisory simulation: warnings must be acknowledged; no dry run available is stated, not blocking. Authorize is ref-guarded; the run then shows as Running and the page asks every 2 s (KeeperHub's hint) for up to 5 minutes, then refreshes the tape; a receipt fires the first-receipt welcome.
- **Copy.** Page subtitles, empty and error states, the Getting started steps and configuration comments, the MCP setup copy, "Use <label>." prompts, the automation status names, the Run now card's lines and the run messages are the agent's wording.

## 8 · Automations from chat
Planned with Abu 2026-09-13 (decisions 19–22). Full plan: `~/.claude/plans/snazzy-baking-blossom.md`.
Sources: KeeperHub fork `lib/mcp/tools.ts:875-1063,1183-1300,1969-2000,2746-2779` (workflow CRUD, execute, validate_cron, validate_workflow), `lib/mcp/workflow-schema-constants.ts:169-303` (triggers), `app/api/workflows/create/route.ts`, `app/api/workflows/[workflowId]/route.ts` (PATCH checks), `app/api/chains/[chainId]/abi/route.ts`, `docs/api/workflows.md:103-143` · slice 6 write card · slice 7 automation detail. No reference has an automation proposal card: drawn from the write card.
Chat shape (server turns it into KeeperHub nodes): `{ name, description?, trigger: manual | schedule{cron,timezone?} | block{network,every} | event{network,contract,event,abi?} | webhook | tempo-payment{network,token,recipient,memo?}, steps: [{ action: opId | "condition", label?, params }] }`. Straight line; condition continues on true; node ids `trigger`, `step-N`; references `{{@step-N:Label.field}}`.

**Part A: build and switch on from chat**
- [x] **8.0 Paperwork.** DECISIONS 19–22; FINDINGS "Creating automations"; this checklist.
- [x] **8.1 Shape ↔ graph (pure).** `lib/automations/build.ts` (chat shape → nodes/edges; graph → chat shape or not-a-straight-line); `lib/automations/proposal.ts` (local check shared by card and server). Tests.
- [x] **8.2 Engine.** `lib/execution/automations.ts`: `list_automations` / `get_automation` reads; previews + broadcasts for `create_automation` and `set_automation_enabled`; `writeIntent` optional `workflowId`, terminal stamps it; recovery closes change intents by reading `get_workflow`. Tests.
- [x] **8.3 Tool surface.** Seven tools in `surface-tools.ts`; `routeToolCall`, `requiresConfirmation`, `buildToolApproval`, `INSTRUCTIONS`, `SIMULATE_TOOLS`, `checkEditedWrite`, voice policy cases. Tests updated.
- [x] **8.4 Cards.** `components/cards/automation-card.tsx` + pure view logic; list/describe cards; `components/automations/step-list.tsx`; edit form; `selectRenderer` + `tool-meta`; `POST /api/automations/[id]/check` and `/enabled`. Tests.
- [ ] **Stop A.** Abu: "every weekday at 09:00 UTC check my org wallet's ETH on Base Sepolia" → card → edit time → Authorize → SAVED · OFF → KeeperHub check → Turn on → LIVE → Live on the board; "what automations do I have?"; desktop + ~390px. *(Abu, 2026-09-13: "let keep building" before trying it. Part B was built next; this walk is the start of Stop B.)*

**Slice 8 part A report (2026-09-13).** Typecheck, lint, build clean. `pnpm test`: 750 passing in 63 files (+61 since slice 7: new `tests/automations/{build,proposal}.test.ts`, `tests/execution/automations.test.ts`, `tests/cards/automation-cards.test.ts`; additions to `tests/execution/recover.test.ts`, `tests/registry/{edited-write,surface-tools}.test.ts`, `tests/chat/route.test.ts`). Dev server restarted fresh on 3001 (`.next` cleared, now run from the Claude session): `/app`, `/app/automations`, `/app/automations/<id>`, `/docs` → 200; `POST /api/automations/<id>/check`, `/enabled`, `/api/chat/simulate`, `GET /api/automations` → 401 signed out. Nothing looked at in a browser, nothing signed in exercised, **no automation saved, checked or turned on against real KeeperHub**: node shapes, `validate_cron`, `validate_workflow`, the ABI lookup and `update_workflow`'s answer are read from its source (`FINDINGS.md`).
Files: `lib/automations/{build,proposal}.ts`, `lib/execution/{automations,errors}.ts`, `components/cards/{automation-card,automation-read-cards}.tsx`, `components/cards/automation-view.ts`, `components/automations/step-list.tsx`, `app/api/automations/[workflowId]/{check,enabled}/route.ts`; changes to `lib/automations/{index,shape}.ts`, `lib/execution/index.ts`, `lib/ledger/index.ts`, `lib/data/ledger.ts`, `lib/registry/{surface-tools,edited-write}.ts`, `lib/voice/policy.ts`, `app/api/chat/{route,simulate/route}.ts`, `components/cards/{card-renderer.tsx,select-renderer.ts,tool-meta.ts,write-card.ts}`, `components/chat/{chat.tsx,chat-rules.ts}`, `components/pages/automation-detail.tsx`.
Differences and choices, all deliberate:
- **Tools (5 → 9).** `list_automations`, `get_automation` run straight away; `create_automation`, `set_automation_enabled` stop on a card. Run, edit and delete land with part B. A proposal the check rejects never stops for the person: the approval gate lets it through to execute, which refuses it as `validation_failed` so the model repairs it (execute re-runs the same check, stricter for an event ABI, so an unchecked proposal cannot save). That returned call stays inside the tools line with no card.
- **Chat shape → KeeperHub graph** (`lib/automations/build.ts`): node ids `trigger`, `step-N`, distinct positions so KeeperHub's editor does not stack them, a condition's next edge on `sourceHandle: "true"`, a protocol action's hidden routing settings carried on the node as a direct run carries them. Reading back refuses branches, loops, switched-off steps and other system steps.
- **Proposal check** (`lib/automations/proposal.ts`), beyond KeeperHub's own: unknown action, system or unavailable action, a field the action lacks, missing or malformed values (template references pass), text fields ending in "Address" checked as addresses (KeeperHub's transfer step refuses anything `ethers.isAddress` rejects), a network outside the action's list, a step reading a later step, 5–6 field cron, IANA timezone, Tempo networks 4217/42431, event name in the ABI. Warnings (webhook key, a non-wallet connection) never block.
- **Engine (sixth change to v1's lib).** Every change: intent before KeeperHub, terminal after; opIds `workflow/create|enable|disable`; `writeIntent` takes `workflowId`, `writeTerminal` stamps a saved automation's id; `writeRunIntent` takes an `opId` for the card's Turn on (no conversation or tool call, like Run now). Saves always send `enabled: false` and the tool call id as idempotency key; the ABI is fetched when missing and kept out of the ledger and the model's context. Recovery closes an enable/disable by reading the automation back; a save that never answered waits out the 24 h expiry. `toErrorOutput` now shares `toExecutionError` (`lib/execution/errors.ts`).
- **Previews.** Create: KeeperHub's reading of the cron, the ABI and event, which steps move value, "switched off until you turn it on". Turn on: KeeperHub check (errors block), dry run (warnings need a tick), a warning naming every value-moving step, and blockers for deactivated, on demand or already in that state.
- **Card** (additive, no reference): the write card's anatomy around the start and the numbered step rail extracted from the detail page (`StepList`, now shared). Meta ladder AWAITING AUTHORIZATION → EDITED → SAVING → SAVED · OFF → LIVE, or TURNING ON/OFF → TURNED ON/OFF; stamps SAVED, LIVE, ON, OFF. The saved card runs KeeperHub's check itself and offers Turn on (routes `check` and `enabled`, callbacks in `chat.tsx`); on demand shows how it runs instead; a read-only chat shows neither. Edit (decision 11): name, the start's settings, every step's fields through the existing `EditPanel`; the kind of start and which steps run stay fixed, and `checkEditedWrite` re-checks on the server.
- **Read cards.** List in the capability card's list grammar with the board's status badge; describe with status, start, steps and the last three runs, plus why it cannot be edited here when it branches.
- **Known until part B.** Activity names the new rows by their raw op id (`workflow/create`); the detail page has no Turn on/off yet; a recovered automation change toasts with the generic "landed" wording.
- **Copy.** Tool descriptions, the prompt line, every card line, the check block labels, the "moves value" pill and all messages are the agent's wording.

**Part B: the rest of the powers**
- [x] **8.5** `run_automation` (chat-started run carries conversation + tool call), `update_automation`, `delete_automation`, `set_automation_enabled` for existing automations; card states.
- [x] **8.6** Detail page Turn on / Turn off (same card in `ui/dialog`); Activity labels + outcome words for `workflow/*`; board empty state "Create one in chat →".
- [x] **8.7 Landing tabs** (decision 22): Copilot `.env.local` block + Vercel `vercel env add` lines from one constant shared with `/docs`.
- [x] **8.8 Checks.** Typecheck, lint, test, build; restart dev server; new routes 401.
- [ ] **Stop B.** Abu (signed in, desktop + ~390px): Stop A's walk first, then "run it now" → RUNNING → EXECUTED; "turn it off"; "rename it and change the time to 10:00" → What changes → SAVED; Turn on / Turn off on its page; "delete it" → DELETED, gone from the board; Activity shows each row by name; the landing's Copilot and Vercel tabs.

**Slice 8 part B report (2026-09-13).** Typecheck, lint, build clean. `pnpm test`: 777 passing in 65 files (+27 since part A: new `tests/automations/diff.test.ts`, `tests/pages/copilot-env.test.ts`; additions to `tests/execution/{automations,recover}.test.ts`, `tests/automations/build.test.ts`, `tests/registry/{surface-tools,edited-write}.test.ts`, `tests/chat/route.test.ts`, `tests/cards/automation-cards.test.ts`, `tests/pages/activity.test.ts`). Dev server restarted fresh on 3001 (`.next` cleared, run from the Claude session): `/`, `/app`, `/app/automations`, `/app/automations/<id>`, `/app/activity`, `/docs` → 200; `POST /api/automations/<id>/check`, `POST …/enabled`, `GET /api/automations/runs/<id>`, `POST /api/chat/simulate`, `GET /api/automations` → 401 signed out. Nothing looked at in a browser, nothing signed in, **no automation changed, run or deleted against real KeeperHub**: `update_workflow`'s checks, the DELETE route's `force` rule and its acceptance of the OAuth Bearer are read from its source (`FINDINGS.md`).
Files: `lib/automations/diff.ts`, `components/docs/copilot-env.ts`; changes to `lib/automations/{build,index,shape}.ts`, `lib/execution/{automations,index}.ts`, `lib/registry/{surface-tools,edited-write}.ts`, `lib/voice/policy.ts`, `lib/activity.ts`, `app/api/chat/{route,simulate/route}.ts`, `app/api/automations/[workflowId]/check/route.ts`, `components/cards/{automation-card.tsx,automation-view.ts,write-card.ts,tool-meta.ts}`, `components/chat/{chat.tsx,chat-rules.ts}`, `components/pages/{automation-detail,automations,activity}.tsx`, `components/shell/write-recovery.tsx`, `components/landing/how-install.tsx`, `app/(app)/docs/page.tsx`, `docs/FINDINGS.md`.
Differences and choices, all deliberate:
- **Tools (9 → 12).** `update_automation` takes the whole automation in the create shape plus its `workflowId`; `run_automation` and `delete_automation` take the id. Each stops on its card once it names an automation (a change must also pass the proposal check); a broken one goes back to the model as in part A.
- **Change.** The preview reads the automation back. One that is not a straight line is blocked with the reason. "What changes" lists old → new (`lib/automations/diff.ts`: name, start, each step's fields; values cut at 48 characters unless cutting would make old and new read the same); nothing changed is a blocker; a live automation with value-moving steps gets a warning that needs a tick. The description and a pasted event ABI (same contract) carry over when the proposal leaves them out. Save: intent `workflow/update` → `update_workflow` with name, description, nodes and edges, never `enabled` → terminal. Reading back renumbers an editor-made automation's nodes `trigger`/`step-N` and rewrites `{{@id:…}}` references to match.
- **Run.** Preview: KeeperHub's dry run (warnings need a tick), the steps that move value, "Every step runs now, without stopping again"; deactivated blocks. `startWorkflowRun` takes `chat: { conversationId, toolCallId }`: the intent is keyed by the tool call and so is the idempotency key; Run now on the page is unchanged. The card follows `/api/automations/runs/[ledgerId]` every 2 s for up to 5 min: RUNNING → EXECUTED with the tx hash, FAILED with a VOID stamp, or STILL RUNNING; a read-only chat says STARTED. The first-receipt welcome fires only when the card watched the run land.
- **Delete.** Preview: status, last update (UTC), run history ("N runs, deleted with it", "20+" when KeeperHub's list is full), "This can't be undone". REST `DELETE /api/workflows/[id]?force=true` with the session's Bearer, because MCP `delete_workflow` cannot force and KeeperHub refuses a workflow that has run without it; its own editor does the same after "Delete Workflow and All Runs". A 404 counts as already gone; no answer leaves the intent open, and recovery closes it once the automation reads back as gone. Open question in `DECISIONS.md`.
- **Switch cards.** The chat's on/off, run and delete cards show the automation itself (start, status, steps) from the preview's `subject`, before Authorize and while sending.
- **Recovery.** `workflow/update` closes when KeeperHub holds exactly the confirmed version (name, description and the rebuilt graph); `workflow/delete` when it is gone. Recovery toasts use Activity's names; an automation change no longer fires the first-receipt welcome (a run still does).
- **Detail page.** Turn on / Turn off (ghost pill beside Run now, only for Live or Off) opens the Masayume dialog with the write card's anatomy over `POST /check { enabled }` and the chat card's `PreviewBlock`, switches through `/enabled`, then reloads. `/check` takes an optional `enabled` body (absent means on, as the chat card sends).
- **Activity.** Names: New automation, Automation changed, Automation turned on / off, Automation deleted, with the automations mark; outcome words per action: Saved, On, Off, Deleted, Executed, and Saving, Turning on / off, Deleting, Running while open (`outcomeOf(state, opId)`).
- **Board.** Empty state: "Create one in chat →" (`/app?prompt=Help me create an automation.`, sent once on arrival) beside a ghost "Open KeeperHub ↗".
- **Landing tabs (decision 22).** `components/docs/copilot-env.ts` is the one list: the 7 settings `lib/config.ts` requires, defaults left out. Copilot tab = `.env.local` without comments; Vercel tab = one `vercel env add NAME production` per setting; the docs' Getting started renders the same list with comments. The install subtitle said the copilot reads a `kh_` key, which it does not; reworded. Shell, Claude Code and Cursor unchanged.
- **Voice policy.** Change and delete confirm as config changes; a run confirms as value-moving.
- **Known.** History's executed count includes automation saves, changes and deletes (they are receipts in the ledger).
- **Copy.** Tool descriptions, the prompt line, every new card line, the stamps, the outcome words, the empty state and the install subtitle are the agent's wording.

## 9 · Voice
Planned with Abu 2026-09-13 (decisions 26–30). Full plan: `~/.claude/plans/nifty-stirring-ripple.md`.
Shape: `gpt-realtime-2.1` over WebRTC with a server-minted key; every voice tool posts to our own routes; a voice write becomes the normal chat card (signed `approval-requested` part stored as the last message), voice is told "waiting, NOT done" and the mic mutes; when the card resolves, a watcher in `Chat` hands the outcome to voice (system message + `requestResponse`). The SDK's `needsApproval` / `approve()` is not used.
- [x] **9.0 Paperwork.** DECISIONS 27–30; FINDINGS "Voice (Realtime)"; this checklist.
- [x] **9.1 Prove the unknowns first** *(2026-09-13, all three passed: see FINDINGS "Voice spikes")* (server-side scripts against the real API): (a) agents-js `tool()` accepts our JSON Schemas (`$schema` stripped); (b) a voice-written approval part with no provider metadata resumes through `/api/chat` on OpenAI Responses; (c) a system message + `requestResponse` makes the realtime model speak the outcome. Any failure → stop and tell Abu.
- [x] **9.2 Engine.** `lib/voice/policy.ts` (listing-payment blocks, config-management reason, 12 tools, prompt: wait / never say done / no hex aloud / short / English for now); `lib/voice/{instructions,proposal,outcome}.ts`; `surfaceRealtimeTools` without `$schema`; optional `OPENAI_REALTIME_MODEL` / `OPENAI_REALTIME_VOICE`. Tests.
- [x] **9.3 Routes.** `app/api/voice/{session,tool,transcript}` (session gate, live-conversation check, `validateUIMessages` + `persistMessages`); chat resume adds the one-short-sentence line for a voice proposal. Tests.
- [x] **9.4 Browser session.** `components/voice/use-voice-session.ts` (key → `RealtimeSession`, own mic stream + audio element, tools → `/api/voice/tool`, transcript saving, mute/end, levels, 9/10-minute timers, errors) + `voice-rules.ts` with tests.
- [x] **9.5 Chat wiring.** Composer mic (signed in, not archived), composer paused "Voice is on"; `chat.tsx` start, `setMessages` merge, outcome watcher (`chat-rules.ts`, tested), close on archive/conversation change; mic mark on said messages.
- [x] **9.6 Orb.** `components/voice/voice-orb.tsx`: Masayume SenseiDock anatomy (pill, avatar, ring draining over the 10 minutes, pulse = mic or voice level), states connecting / listening / speaking / waiting for your card / ended / error; tap → Mute / End; Portaldot paint; clear of send, phone pill and toasts.
- [x] **9.7 Checks.** Typecheck, lint, test, build; restart dev server; `/api/voice/*` 401 signed out.
- [x] **9.8 Composer + voice redesign (decision 31).** *(2026-09-13: typecheck, lint, voice + chat tests (121) clean; `components/voice/{voice-glow,voice-bar,voice-hero}.tsx` replace `voice-orb.tsx`; `components/chat/composer.tsx` rewritten; engine reports five frequency bands (`toBands`); styles in `app/(app)/shell.css`. Not looked at in a browser.)* Composer: a pill that springs open on focus or typing and settles when empty (ai-chat-input physics), a scroll fade, the plus menu and network chip in the opened row, one action button morphing voice → send. Voice: the composer becomes a glowing voice bar (level-reactive gradient orb, live frequency bars, status, clock, mute, End; the glow ring turns pending while a card waits, destructive on error); an empty chat shows a large glowing orb with the status under it. The floating side orb is removed. Checks.
- [ ] **Stop.** Abu, signed in, desktop + ~390px: price by voice (card + spoken + transcript); "send 0 ETH to myself on Base Sepolia" → card, waiting, mic muted → Authorize → spoken result + one chat line; the same with Cancel; mic denied; End; the 10-minute goodbye (short test cap: `VOICE_SESSION_SECONDS=90` in `.env.local`, then restart the dev server).

**Slice 9 report (2026-09-13).** Typecheck, lint, build clean. `pnpm test`: 802 passing in 68 files (+25 since slice 8 part B: new `tests/voice/{policy,routes,voice-rules}.test.ts`; additions to `tests/registry/surface-tools.test.ts`). The three live spikes passed first (FINDINGS "Voice spikes"). Dev server restarted fresh on 3001 (`.next` cleared, run from the Claude session): `/app`, `/app/c/<id>`, `/docs` → 200; `POST /api/voice/session`, `/api/voice/tool`, `/api/voice/transcript` → 401 signed out. Nothing looked at in a browser, **no voice session run with a microphone, no voice card authorized, nothing sent to KeeperHub**; the WebRTC path, the level meter and the spoken outcome after a real card are unproven until Abu's walk.
Files: `lib/voice/{instructions,outcome}.ts`, `app/api/voice/{session,tool,transcript}/route.ts`, `app/api/voice/_shared/voice.ts`, `components/voice/{voice-engine,voice-rules,use-voice-session}.ts`, `components/voice/voice-orb.tsx`; changes to `lib/voice/policy.ts`, `lib/config.ts`, `lib/registry/surface-tools.ts`, `app/api/chat/route.ts`, `components/chat/{chat.tsx,chat-rules.ts,composer.tsx,message-list.tsx}`, `app/(app)/shell.css`, `docs/{DECISIONS,FINDINGS}.md`.
Differences and choices, all deliberate:
- **Policy.** `voiceOutcomeFor`: a read runs; only the classes the chat's confirm ceremony can authorize become cards; `listing-payment`, `mixed-effect` and `quarantined` block. `config-management-write` has its own reason. The prompt is rewritten in OpenAI's realtime sections: short replies, a line before each tool call, search first, propose then say once it is waiting and STOP, never say done unless a system message reports it, no addresses or hashes read aloud, English only for now (decision 24), a goodbye when told the session is ending. `voiceInstructions` adds the header's network, as the chat does.
- **Config.** `getVoiceConfig()`: `OPENAI_REALTIME_MODEL` (default `gpt-realtime-2.1`), `OPENAI_REALTIME_VOICE` (default the model's), `VOICE_SESSION_SECONDS` (60–3600, default 600). All optional, so not in the settings list.
- **Session route.** Mints a 120-second `ek_` key with the model, instructions and voice; returns them with the twelve tools (no `$schema`) and the session length. The live-conversation check (the 30-minute rule) now runs on the server for all three voice routes.
- **Tool route.** Blocked calls are refused and not stored. A second card while one waits is refused. A change (`requiresConfirmation`, schema-checked) is stored as the chat's signed `approval-requested` part with `metadata.source: "voice"`, after the speech that came before it; voice is told "has NOT happened … stop and wait"; the response carries `cardToolCallId`. Anything else runs through `routeToolCall` and is stored as its card; a `validation_failed` result is not stored, so voice repairs it. The message builders live in `app/api/voice/_shared/voice.ts` instead of `lib/voice/proposal.ts`, beside the chat's signer.
- **Transcript route.** Finished speech is stored as text messages with stable ids `voice-<realtime item id>`; nothing is stored while a card waits (the chat's lock and the approval resume read the last message).
- **Chat resume.** When the waiting card came from voice, the chat model gets `VOICE_RESUME_LINE` and replies in one sentence (decision 28).
- **Browser engine.** `VoiceEngine` (outside React, read with `useSyncExternalStore`): mic opened first, then the key, then `RealtimeSession` over WebRTC with our own stream and audio element, `gpt-4o-mini-transcribe` and semantic turn detection. Tools only post to `/api/voice/tool`; before posting, the engine waits up to 2.5 s for the person's words to finish transcribing, so they land before the card. A waiting card mutes the mic (decision 30). The chat watches that card's tool part; once it has ended and the chat is idle, `voiceOutcome` gives voice one plain line as a system message plus a reply request, and the mic comes back. A wrap-up system message at 9 minutes, the end at 10 (decision 29), a toast when time ends or the connection drops. Levels come from Web Audio analysers: the mic while listening, the remote track while speaking.
- **Chat.** The composer's mic starts and ends voice (available when nothing is sending or waiting, or to end voice); typing pauses with "Voice is on. End voice to type."; voice creates the conversation on first use, like a typed message (`ensureConversation`); voice ends when the chat archives or unmounts. Spoken messages carry a small SAID mark.
- **Voice button.** Masayume SenseiDock: dark pill bottom right, label sliding out on hover (hidden on phones), 72-unit ring with R = 30. The ring drains over the session, the pulse ring follows the live level, and the teaser bubble with its tail appears only while a card waits (pending) or voice could not start (destructive). Tapping opens Mute / End (or Try again / Close). Portaldot paint; 9.5 rem above the bottom on desktop, 14 rem on phones to clear the composer and floating nav.
- **Known.** Speech said while a card waits is not saved. A conversation used only by voice keeps the "New conversation" title (auto-titling runs on typed turns). Words that take longer than 2.5 s to transcribe can be saved after the card, unless the card is still waiting. The ten-minute cap is kept by the browser (an open realtime session outlives its key). The remote-track level meter is standard Web Audio, not documented by the SDK.
- **Copy.** The voice prompt, tool results voice hears, outcome lines, button labels, bubble text, composer hint, SAID mark and toasts are the agent's wording.

## 9b · Voice follow-ups
Planned with Abu 2026-09-13 (decisions 32–35) after trying voice. Full plan: `~/.claude/plans/nifty-stirring-ripple.md`.
- [x] **9.9 Chat scroll.** The chat column is pinned to the viewport (`fixed`, top under the header/strip); the list contains its own scroll and autoscrolls only itself. Other pages untouched.
- [x] **9.10 Org wallet.** `lib/session/org-wallet.ts` (EVM + Solana, cached per org) into the chat and voice instructions; new read tool `get_org_wallet_balances` with a Holdings card.
- [x] **9.11 Direct-run gaps.** `runsDirectly(entry)` (protocol actions only, as KeeperHub's execute route); search marks the rest automation-only with a reroute note; execution refuses them before any card.
- [x] **9.12 Form cards in chat.** Tool `request_input` (no server execute; 14 tools), `lib/chat/input-request.ts` checks, answer path in the chat route (stored part only), `InputRequestCard`, composer lock, auto-send predicate.
- [x] **9.13 Forms in voice.** Voice tool route form branch, `POST /api/voice/answer` (no chat model), engine waiting kind, `voice-form-sheet.tsx` above the voice bar, outcome line with the values.
- [x] **9.14 Checks.** Typecheck, lint, test, build; restart dev server; new routes 401.

**Slice 9b report (2026-09-13).** Typecheck, lint, build clean. `pnpm test`: 862 passing in 75 files (+60 since slice 9: new `tests/chat/{input-request,form-rules,form-answer}.test.ts`, `tests/voice/form-routes.test.ts`, `tests/session/org-wallet.test.ts`, `tests/execution/holdings.test.ts`, `tests/registry/runs-directly.test.ts`; `tests/execution/route.test.ts`, `tests/chat/{route,rerun}.test.ts` moved their write examples from `web3/transfer-token` to `aave-v3/supply`, since KeeperHub only runs web3 steps inside automations). Dev server restarted fresh on 3001: `/app` 200; `/api/voice/{answer,tool,session}`, `/api/chat`, `/api/account` 401 signed out. **Nothing looked at in a browser, nothing signed in exercised, no voice session opened, no model call made.** Notes for the walk:
- The chat route now refuses a new message (409) while a card or form waits. The composer already blocked it; this closes the server side.
- A form answered in chat posts with the conversation id on the answer itself (`addToolOutput` options), not relying on the chat's earlier request.
- The "Use my org wallet" shortcut uses the EVM address and is hidden when the form's network is Solana.
- `get_org_wallet_balances` with no network lists only networks holding something; asked about one network it shows every token, zeros dimmed.
- [ ] **Stop.** Abu: long chat scroll (composer stays); org wallet holdings without an address question; typed "send some ETH to a friend" → form → transfer card; the same by voice with the pop-up; cancel a form in both; desktop + ~390px.

## 10 · More add-ons
Planned 2026-09-14 (decisions 36–37). Full plan: `~/.claude/plans/nifty-stirring-ripple.md`.
- [x] **10.1 ⌘K palette.** `CommandPaletteProvider` in the app layout (⌘K / Ctrl+K, header button, composer plus-menu), pure `palette-items.ts` search (Pages · Networks · Actions), `/api/catalog` loaded on first open, `chat-inbox.ts` so an action's prompt reaches the open chat or `/app?prompt=`.
- [x] **10.2 Shareable receipts.** `receipt_share` table (migration 0003), `lib/data/shares.ts` (session-scoped, plus the one token read), pure `lib/shares.ts` whitelist, `/api/receipts/share` GET/POST/DELETE, public `/r/[token]` page with not-found and preview image, Share menu on the write card receipt, Activity and the run card.
- [x] **10.3 Checks.** Typecheck, lint, test, build; `db:migrate` + `db:verify`; restart dev server; `/api/catalog` 200, `/api/receipts/share` 401, `/r/unknown` 404.

**Slice 10 (parts 1–2) report (2026-09-14).**
- **Checks:** typecheck, lint and build clean. `pnpm test`: 888 passing in 80 files (+26 since 9b):
  - new `tests/shell/palette-items.test.ts`, `tests/chat/chat-inbox.test.ts`, `tests/pages/palette-catalog.test.ts`
  - new `tests/shares/{view,routes}.test.ts`
- **Database:** migration `0003_old_hannibal_king.sql` (table `receipt_share`) applied to `keeperhub_v2`; `db:verify` passes.
- **Install:** `node_modules` was missing this morning (it existed on 2026-09-13; cause unknown). It was reinstalled with `pnpm install --frozen-lockfile`, and nothing in `package.json` or the lockfile changed.
- **Dev server on 3001:**
  - `/app` 200
  - `/api/catalog` 200 (436 actions, 8 starters, 126 KB)
  - `/api/receipts/share` GET/POST 401 signed out
  - `/r/unknown` and a well-formed fake token 404 with the "This receipt isn't shared" page, `noindex`
  - the preview image renders (checked the PNG)
- **Not done:** nothing looked at in the browser, nothing signed in exercised, no link made for a real receipt.

Notes for the walk:
- An action picked in the palette goes to the chat on screen (sent, or left in the composer when the chat is busy or a card waits). With no chat on screen it opens `/app?prompt=`.
- The composer's plus-menu item opens the palette straight from the menu. If focus lands wrong there, say so.
- Share appears only on receipts with a transaction: the write card, the run card once executed, and Activity rows (icon only).
- The link is made on the first Copy, never just by opening the menu. Stop sharing kills that link for good; sharing again makes a new one.
- [ ] **Stop.** Abu: ⌘K "aave" → Supply → chat asks with a form; ⌘K "sepolia" switches network; ⌘K "activity" opens it; Share a sent transfer → Copy link → private window shows the public receipt; Stop sharing → link off; paste the link in X/Slack for the preview; desktop + ~390px.
- [x] **10.4 Languages part 1** (decisions 38–40; plan `~/.claude/plans/nifty-stirring-ripple.md`). `lib/locale.ts` (13 locales, `kh_locale` cookie, `languageRules`), `LocaleProvider`, language dialog from the account menu, a Languages section in ⌘K, chat instructions + auto-title in the picked language, voice `# Language` section (English-only line removed) + transcription language, commas refused in typed amounts.
- [x] **10.5 Checks.** Typecheck, lint, test, build; restart dev server; `/api/voice/session` 401.

**Languages part 1 report (2026-09-14).**
- **Checks:** typecheck, lint and build clean. `pnpm test`: 898 passing in 81 files (+10):
  - new `tests/shell/locale.test.ts`
  - additions to `tests/voice/{policy,routes}.test.ts`, `tests/chat/route.test.ts`, `tests/cards/{money,money-mover}.test.ts`, `tests/shell/palette-items.test.ts`
- **Dev server on 3001:** `/app` 200 with and without `kh_locale=ja`; `/api/voice/session` and `/api/chat` 401 signed out.
- **Not tested yet:** nothing looked at in a browser, nothing signed in, no model or voice call made. So there's no evidence yet on how each language sounds or whether voice slips back to English.

Notes for the walk:
- **Where the language applies:** the chat instructions and the auto-title prompt now end with the picked language's rules. The voice instructions have a `# Language` section, the English-only line is gone, and the realtime transcript is told the language code.
- **What stays English:** tool descriptions, errors fed back to the model, the voice outcome lines and the screens (part 2).
- **Comma amounts:** a typed amount with any comma is refused everywhere it is parsed or checked. That includes "1,000", which was quietly accepted before.
- **Display vs input:** displayed amounts still group with commas. Edit fields start from the raw amount, so nothing displayed is parsed back.
- [ ] **Stop.** Abu: pick Español → Spanish reply + title, English card; form labels in Spanish; "0,5" refused; voice in Spanish, Japanese, Hindi (note any slip to English); ⌘K "japanese"; switching during voice applies next session.
- [ ] **Languages part 2: the screens** (decisions 41–42; plan `~/.claude/plans/nifty-stirring-ripple.md`).
  - [x] **11.1 Foundation.** next-intl without URL prefixes; one resolver (cookie → browser language → English) for screens, chat and voice; messages split per area in `messages/<locale>/<area>.json`; providers in the root layout with `<html lang>`; language refresh; landing footer picker; fonts (Geist latin-ext/cyrillic/vietnamese, Noto Sans JP/KR/SC/Devanagari per language); `Translate` passed into pure copy modules with English by default.
  - [x] **11.2 English into messages.** Areas: common/errors, metadata, landing, shell, chat, cards, voice, pages. Docs, /dev, the share image, tool descriptions and prompts stay English.
  - [x] **11.3 Translations.** `messages/GLOSSARY.md`; 12 locale folders; `tests/i18n/messages.test.ts` (same keys, compiles, same arguments, protected terms).
  - [x] **11.4 Checks.** Typecheck, lint, test, build; dev server; curl `lang="ja"` and `Accept-Language: es`.
  - **Languages part 2 report (2026-09-15).**
    - **Checks:** typecheck, lint and build clean. `pnpm test`: 953 passing in 82 files, including `tests/i18n/messages.test.ts`: every language has the same keys, arguments and tags as English, formats, and keeps "KeeperHub" and "⌘K".
    - **How the text moved:** 1,055 English messages moved into `messages/en/<area>.json` (10 areas) by six area agents, which kept every existing English test unchanged. Pure copy modules take a translator as their last argument, English by default. The 12 languages were written by one agent each from `messages/GLOSSARY.md`, and their open wording questions are in `messages/REVIEW-NOTES.md`.
    - **Dev server on 3001:**
      - `/` with `Accept-Language: es` gives `lang="es"` and the Spanish landing.
      - `/app` with `kh_locale=ja` gives `lang="ja"`, Japanese text and the title "チャット · KeeperHub Copilot".
      - `/app/activity` with `kh_locale=hi` and a Spanish browser gives Hindi.
    - **Not verified yet:** nothing looked at in a browser, nothing signed in.
    - **Deviation from the plan:** Japanese, Korean, Chinese and Hindi use each device's system fonts through `:lang` rules. next/font's Noto Sans JP/KR/SC/Devanagari built, but broke `next dev` (Turbopack couldn't resolve their hundreds of font slices).
    - **Wiring across areas:** the tools line, write recovery, the chat's automation check and the shared receipt label all pass the translator. History shows the "New conversation" sentinel translated. Saving an unchanged rename sends nothing.
    - **Still English by design (decisions 39 and 42):** docs pages, KeeperHub action names, server and KeeperHub error text, model prompts, the share preview image.
  - [ ] **Stop.** Spanish browser → Spanish landing, app and replies; 日本語 / हिन्दी / 한국어 fonts; German cards and pages at ~390px; amounts and addresses unchanged; wording notes for reviewers.

## 11 · Proof
- [x] **One real transaction through the app on hosted KeeperHub.** Abu, 2026-09-17 on https://keeperhub-copilot-v2.vercel.app: two sponsored writes on Ethereum Sepolia went through the card and landed on chain — `deposit` 0.01 ETH to WETH (`0xe00f48977c…`) and `approve` of the Aave pool (`0xbdc610fc1f…`), both stored with KeeperHub's receipt `status: completed`. The planned shape was a Base Sepolia self-transfer; what he actually ran proves the same path (dry run → Authorize → KeeperHub → receipt → Activity).
- [x] **Source link:** https://github.com/Blockchain-Oracle/keeperhub-copilot-v2 (private, created 2026-09-17; v1's repo is private too). Flip to public before judging with `gh repo edit Blockchain-Oracle/keeperhub-copilot-v2 --visibility public`.
- [ ] Demo video.

## 12 · Hosted on Vercel
Done 2026-09-15 (decision 43). Public address: **https://keeperhub-copilot-v2.vercel.app**.
- [x] **12.1 Own Vercel project `keeperhub-copilot-v2`** (team blockchain-oracles-projects), Next.js preset, Node 24, linked from the repo root (`.vercel/`, gitignored). Deploy: `vercel deploy --prod --yes` from the root. v1's project `keeperhub-copilot` and its address were not touched (decision 9).
- [x] **12.2 Settings** (the eight from `components/docs/copilot-env.ts`, production, sensitive): the same `keeperhub_v2` database as local, issuer `https://app.keeperhub.com`, a second KeeperHub sign-in client "KeeperHub Copilot v2 (hosted)" `7a0b5236-…` registered by KeeperHub's open client registration for `https://keeperhub-copilot-v2.vercel.app/api/auth/callback`, `APP_BASE_URL` = the public address, v1's OpenAI key, fresh session and approval secrets (the local ones are unchanged).
- [x] **12.3 `.vercelignore`** (new file at the root): keeps `references/`, `legacy/`, `docs/`, `tests/`, env files and build output out of the upload (478 files go up). Folder names are root-anchored: a bare `docs` also dropped `components/docs` and broke the first build.
- [x] **12.4 Checks on the public address.** `/`, `/app`, `/app/{activity,automations,history}`, `/docs`, `/api/catalog`, `/api/platform/chains` (24 networks) 200; `/api/account`, `/api/chat`, `/api/voice/session`, `/api/receipts/share` 401 signed out; `/r/unknown` 404 (the database answers). `Accept-Language: es` → `lang="es"`; `kh_locale=ja` → the Japanese title. `/api/auth/login` 307s to KeeperHub with the hosted client and callback. Looked at in a browser signed out, desktop and 390px: landing and `/app` (tutorial) render as locally; the only console 404 is `/favicon.ico`, which the app does not ship anywhere.

**Hosting report (2026-09-15).** Local typecheck, lint, build and `pnpm test` (953 in 82 files) clean before deploying. Two Vercel-side stumbles, both fixed: the unanchored `.vercelignore` line (12.3), and the project being created without a framework preset, which made Vercel publish the build as a static site (every route `NOT_FOUND`, only `public/` served) until the preset was set to Next.js. **Not done:** nobody has signed in on the hosted address yet, so the OAuth round trip through KeeperHub's consent page and the callback is unproven there; no model or voice call made on the hosted app.
- [x] **12.5 Fix: write cards never reached KeeperHub** (found by Abu on the hosted app, 2026-09-15). Authorize, Cancel and Try again answered "Your answer did not reach KeeperHub" because the route got the decision with no conversation id and refused it with 400. The AI SDK posts a card's decision by itself and sends only the options given with it; the card callbacks gave none (form answers did). Now `cardDecision` in `components/chat/chat-transport.ts` carries the id, and the transport moved to that file. New `tests/chat/chat-transport.test.ts` drives the real SDK `Chat` and checks the posted body (3 of its 4 fail on the old code). Typecheck, lint, build, `pnpm test` 957 in 83 files; redeployed, and the shipped chat code carries the id. **Not verified:** a real card authorized end to end, since that needs Abu signed in; an Aave borrow also needs collateral supplied first, and its amount is in the token's smallest unit.
- [~] **Stop.** *(2026-09-17: Abu signed in on the hosted app and ran writes — the hosted sign-in round trip and hosted execution are proven. The rest of this walk is still open.)* Abu: open https://keeperhub-copilot-v2.vercel.app, sign in with KeeperHub (first proof of the hosted callback), ask a price, send 0 ETH to yourself on Base Sepolia through the card, try voice; desktop + phone. Decide whether v1's address `keeperhub-copilot.vercel.app` should point at v2 later.

## End-to-end walk (after slice 9)
Landing type → sign-in modal → OAuth → arrive with draft auto-sent → read shows "Used N tools" +
card → "send 0 ETH to myself on Base Sepolia" → write card → edit → dry-run → approve → EXECUTED +
explorer link → same receipt in History replay and Activity → voice: ask price, ask to send → card
appears, voice cannot approve → create automation from chat → appears on `/app/automations`.
